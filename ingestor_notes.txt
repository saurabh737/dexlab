# Run Docker container as non-root

## User must be set in the Dockerfile

docker run -i -t -p 8000:8000 -d --name pyart --restart always --mount target=/app continuumio/miniconda3

# arm_pyart is test with Python 3.8

conda create -n pyart_env -c conda-forge python=3.8 arm_pyart

conda activate pyart_env

pip install flask

python ingestor.py
